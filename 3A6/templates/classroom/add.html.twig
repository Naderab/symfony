{# {{ form(formClass) }} #}

{{ form_start(formClass) }}
    {{ form_label(formClass.name,'le nom de class :') }}
    {{ form_widget(formClass.name) }}
    {{ form_errors(formClass.name) }}
{{ form_end(formClass) }}